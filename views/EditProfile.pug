extends layouts/main 

block content
    .form-container
        form(action= `/users${routes.editProfile}`, method = "post")
            label(for= "avatar") Avatar
            input(type = "file", id= "avatar",  name= "Email", required= true)
            input(type = "text", placeholder= "Name", name = "name", required= true)
            input(type = "email", naem= "Email", name = "email", required= true)
            input(type = "submit", value = "Update Profile", required= true)
        a.form-container__link(href = `/users${routes.changePassword}`) 
            button Change Password